function negativeOnly() {
  let num;
  do {
    num = prompt("Please enter a negative number");
  } while (num >= 0);

  console.log("Thank you for entering a negative number.");
}

negativeOnly();

// Printing a number classified as (n) which has varations of parametres
printOdd(15);

// Printing number combinations from 1 to 15 in which it illicitly stops after breaking a certain
1
3
5
7
9
11
13
15

// Therefore nothing shoul print in equating too the function of printOdd(n)
printOdd("Nothing should print");

// Enabling a function to quit only when an input of a certain text of the user selected choice
parrotuntilquit() 
  let input;
  do {
    input = text(prompt("Enter some text of your choice or the word 'quit'"));
  } while (isNaN(input))
  
  if (isNaN(quit)) {
      console.log("Goodbye!")
  
  return input; }

  // Factorial of a double digit number, than a single digit one, and lastly a decimal
  factorial(15);
  39916800
  factorial(7);
  5040
  factorial(0.5);
  0.0012