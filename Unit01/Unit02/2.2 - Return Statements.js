// Creating a function of a weekday and depending upon a weekend day, it will therefore return true or false
function is_it_the_weekend(day) {
        if (day = 1 || 2) {
          return true;
        } else (day = 3 && 4) 
          return false;}

// Creating a new function of a certain value and seeing if it is divisble by 9 
function evNineIble(value) {
    if (value = x/9 + even) {
        return true;
    }   else (value != x/9 + even)
        return false;}

// Creating a function defining the area of a circle  
function area_circle(r) {
    if (rgiven = x) {  
        return radiusgiven;
    }   else (A = 3.14*x)
        return areaofacircle;} 

// Creating a function defining the area of a cylinder's vertical surface (label) 
function area_label(r, h) {
    if (rgiven = x) {
        return radiusgiven;
    if (hgiven = y) {
    }   return heightgiven;
    }   else (AL=2*π*r*h)
        return areaofacylinderslabel(verticalsurface);} 

// Creating a function defining the surface area of a cylinder 
function area_cylinder(r, h) {
    if (area=t) {
        return radiusgiven;
    }   else (A = 3.14*x)
        return areaofacircle;} 